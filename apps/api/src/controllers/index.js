const auth = require('./auth');
const users = require('./users');
const visitors = require('./visitors');
const occurrenceBook = require('./occurrenceBook');

module.exports = { auth, users, visitors, occurrenceBook };
